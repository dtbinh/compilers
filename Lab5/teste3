programa teste3;

var
    logic a, b, c;
    int x, y, i, n;
    carac car, u;

{
    b := falso;
    c := verdade;
    ler(car);
    ler(u);
    a := verdade;
    escrever(a, b, "isto");
    {
        y := 13;
        x := 3 + y;
    }
    se (i<10) i := i-5;
    enquanto (i>8) i := i / 4;
    repetir n := n+1; enquanto (c);
}
